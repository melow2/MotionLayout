<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jeit.motionlayout">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CodeLabActivity" />
        <activity android:name=".step.Step1Activity" />
        <activity android:name=".step.Step2Activity" />
        <activity android:name=".step.Step3Activity" />
        <activity android:name=".step.Step4Activity" />
        <activity android:name=".step.Step5Activity" />
        <activity android:name=".step.Step6Activity" />
        <activity android:name=".step.Step7Activity" />
        <activity android:name=".step.Step8Activity" />
    </application>

</manifest>